version: '3.0'

services:
  api:
    image: node:alpine
    container_name: gateway-nodejs
    command: "npm run watch"
    working_dir: /usr/src/app
    ports:
      - "3333:3000"
    volumes:
      - ./api:/usr/src/app
