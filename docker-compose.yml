version: '3.0'
name: "gateway"
services:
  api:
    image: node:alpine
    container_name: gateway-nodejs
    command: "npm run watch"
    working_dir: /usr/src/app
    ports:
      - "3000:3000"
    volumes:
      - ./api:/usr/src/app
