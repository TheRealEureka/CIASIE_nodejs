version: '3'

services:
  api-gateway:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - service1
      - service2

  service1:
    image: service1
    ports:
      - "3000:3000"

  service2:
    image: service2
    ports:
      - "3000:3000"
